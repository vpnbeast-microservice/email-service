---

spring:
  cloud:
    config:
      enabled: false
  config:
    activate:
      on-profile: unit-test
  mail:
    username: "changeit"
    password: "changeit"
    host: "localhost"
    port: 3025
    protocol: "smtps"
